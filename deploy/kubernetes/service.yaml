apiVersion: v1
kind: Service
metadata:
  name: syncprimitives
  labels:
    app: syncprimitives
spec:
  type: ClusterIP
  selector:
    app: syncprimitives
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
